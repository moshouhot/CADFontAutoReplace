# v9.2.2 选择性回移决策记录

比较基准：本地 `4ad172f`；上游 `v9.2.2` (`5b259d8`)。

## 已回移

- AWS：抑制状态检测、安装/更新覆盖、AFR 所有权标记、安全 Cleanup、部署器按安装前状态决定是否覆盖。
- 运行时：结构化初始化状态、安装/更新后的 AWS 检查、旧版 AutoCAD 跳过不适用的 AWS 检查、静态 CAD 命令、Hook/映射/检测/替换及日志稳定性修复。
- UI/绑定：注册表 watcher 的 source-generated P/Invoke、ViewModel/UiCommand/字体日志绑定修复。

## 保留的本地架构

- AutoCAD 2013–2027。
- 四个合并 DLL 与 `RuntimeAutoCadPlatform`。
- 绿色目录、`AFR.config.json`、`DeployerCli`、`AFRSELFTEST`。
- 本地静态 `CadDescriptors`；未切换到上游逐年份 sidecar/嵌入 DLL 模型。

## 明确排除

- About 窗口。
- 上游逐年份工程结构。
- 上游 GitHub 工作流与治理脚本。

## 自动验证

- .NET SDK 10.0.201。
- Debug build：0 warning / 0 error。
- Release build：0 warning / 0 error。
- `tests/BackportContracts.Tests.ps1`：通过。
- `tools/Publish-ReleaseAssets.ps1`：成功生成 4 DLL、绿色目录和 ZIP。
- `AFR-Deployer.exe status --json`：`ok=true`，支持列表仍为 2013–2027。
- `doctor --json`：插件、配置和字体资产完整；因检测到当前 `acad.exe` 正在运行，按设计返回 `ok=false`。

## 尚需实机验证

当前未对每个 AutoCAD 年份执行 NETLOAD/AFR/AFRLOG/退出重载矩阵。合入主分支前至少验证 2017、2018、2024、2025、2026、2027；AWS 写入验证必须在关闭所有 `acad.exe` 后进行。
